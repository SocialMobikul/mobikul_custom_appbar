name: mobikul_custom_app_bar_example
description: "Example project for demonstrating the mobikul_custom_app_bar package."
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  mobikul_custom_app_bar:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
